# Dwarf Fortress binary and data — not committed, must be supplied by the user
df-image-base/df/

# Compiled session manager binary
session-manager/session-manager

# Secrets — never commit these (commit the .example files instead)
session-manager/config.yml
session-manager/users.yml
session-manager/users.db
session-manager/users.db-wal
session-manager/users.db-shm

# Docker build cache
.dockerignore

# OS / editor noise
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
